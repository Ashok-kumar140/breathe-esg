import React from "react";
import Template from "../components/Template";

const LoginPage = () => {
  return (
    <div>
      <Template formType="Login" heading="Login" description ="Enter your registered Email ID to continue" />
    </div>
  );
};

export default LoginPage;
